# HTML Basics

- `<h1>` to `<h6>` → Headings  
- `<p>` → Paragraph  
- `<a>` → Links  
- `<img>` → Images  
- `<ul>` / `<ol>` → Lists  

Example:
```html
<h1>Hello World</h1>
<p>This is a paragraph</p>
